<Window x:Class="SystèmeGestionConsultationPrescriptions.Interfaceutilisateur.Views.PatientDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystèmeGestionConsultationPrescriptions.Interfaceutilisateur.Views"
        xmlns:vm="clr-namespace:SystèmeGestionConsultationPrescriptions.Interfaceutilisateur.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="450" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Nom complet"/>
                <TextBox Text="{Binding Patient.Nom, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Date de naissance"/>
                <DatePicker SelectedDate="{Binding Patient.DateNaissance}" Margin="0,5,0,15"/>

                <TextBlock Text="Adresse"/>
                <TextBox Text="{Binding Patient.Adresse}" AcceptsReturn="True" Height="60"/>

                <TextBlock Text="Téléphone"/>
                <TextBox Text="{Binding Patient.Telephone}"/>

                <TextBlock Text="Email"/>
                <TextBox Text="{Binding Patient.Email}"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Annuler" Command="{Binding CancelCommand}" Width="100" Margin="0,0,10,0" Padding="5"/>
            <Button Content="Enregistrer" Command="{Binding SaveCommand}" Width="100" Padding="5"/>
        </StackPanel>
    </Grid>
</Window>
