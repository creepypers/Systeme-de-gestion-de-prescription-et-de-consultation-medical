<Window x:Class="SystèmeGestionConsultationPrescriptions.Interfaceutilisateur.Views.ConsulterPrescriptionDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Consulter Prescription" 
        Height="600" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Colors -->
        <Color x:Key="PrimaryColor">#162640</Color>
        <Color x:Key="SecondaryColor">#3e5369</Color>
        <Color x:Key="AccentColor">#667f92</Color>
        <Color x:Key="LightAccentColor">#86adae</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="LightAccentBrush" Color="{StaticResource LightAccentColor}"/>

        <!-- Styles -->
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Light"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style x:Key="ModernActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C62828"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White"
            Margin="20"
            Padding="30"
            CornerRadius="10">
        <StackPanel>
            <TextBlock Text="Détails de la Prescription"
                     Style="{StaticResource TitleTextStyle}"
                     HorizontalAlignment="Center"/>

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Date -->
                <TextBlock Text="Date :" 
                         Grid.Row="0" Grid.Column="0"
                         Style="{StaticResource LabelTextStyle}"/>
                <TextBlock Text="{Binding DatePrescription, StringFormat=dd/MM/yyyy}"
                         Grid.Row="0" Grid.Column="1"
                         Style="{StaticResource ValueTextStyle}"/>

                <!-- Consultation -->
                <TextBlock Text="Consultation :" 
                         Grid.Row="2" Grid.Column="0"
                         Style="{StaticResource LabelTextStyle}"/>
                <TextBlock Text="{Binding Consultation.Motif}"
                         Grid.Row="2" Grid.Column="1"
                         Style="{StaticResource ValueTextStyle}"/>

                <!-- Médicament -->
                <TextBlock Text="Médicament :" 
                         Grid.Row="4" Grid.Column="0"
                         Style="{StaticResource LabelTextStyle}"/>
                <TextBlock Text="{Binding Medicament}"
                         Grid.Row="4" Grid.Column="1"
                         Style="{StaticResource ValueTextStyle}"/>

                <!-- Posologie -->
                <TextBlock Text="Posologie :" 
                         Grid.Row="6" Grid.Column="0"
                         Style="{StaticResource LabelTextStyle}"/>
                <TextBlock Text="{Binding Posologie}"
                         Grid.Row="6" Grid.Column="1"
                         Style="{StaticResource ValueTextStyle}"/>

                <!-- Durée -->
                <TextBlock Text="Durée :" 
                         Grid.Row="8" Grid.Column="0"
                         Style="{StaticResource LabelTextStyle}"/>
                <TextBlock Text="{Binding Duree}"
                         Grid.Row="8" Grid.Column="1"
                         Style="{StaticResource ValueTextStyle}"/>

                <!-- État -->
                <TextBlock Text="État :" 
                         Grid.Row="10" Grid.Column="0"
                         Style="{StaticResource LabelTextStyle}"/>
                <TextBlock Text="{Binding Etat}"
                         Grid.Row="10" Grid.Column="1"
                         Style="{StaticResource ValueTextStyle}"/>
            </Grid>

            <!-- Bouton Fermer -->
            <Button Content="Fermer"
                    Style="{StaticResource DeleteButtonStyle}"
                    Command="{Binding CloseCommand}"
                    HorizontalAlignment="Right"
                    Margin="0,40,0,0"/>
        </StackPanel>
    </Border>
</Window> 