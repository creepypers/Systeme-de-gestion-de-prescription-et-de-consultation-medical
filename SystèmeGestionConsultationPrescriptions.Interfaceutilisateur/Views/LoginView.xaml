<Window x:Class="SystèmeGestionConsultationPrescriptions.Interfaceutilisateur.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystèmeGestionConsultationPrescriptions.Interfaceutilisateur"
        xmlns:vm="clr-namespace:SystèmeGestionConsultationPrescriptions.Interfaceutilisateur.ViewModels"
        mc:Ignorable="d"
        Title="Connexion" 
        Height="800" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        WindowState="Maximized">

    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#162640"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3e5369"/>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="400"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- PasswordBox Style -->
        <Style x:Key="ModernPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="400"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Button Style -->
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="400"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White"
                Margin="300,84,276,67"
                Padding="50"
                CornerRadius="10">
            <StackPanel VerticalAlignment="Center" Height="571">
                <!-- Doctor Icon -->
                <TextBlock Text="👨‍⚕"
                         FontSize="48"
                         HorizontalAlignment="Center"
                         Margin="0,0,0,20"
                         Height="62"/>

                <!-- Header -->
                <TextBlock Text="Connexion Médecin"
                         FontSize="36"
                         FontWeight="Light"
                         HorizontalAlignment="Center"
                         Margin="0,0,0,10"
                         Foreground="{StaticResource PrimaryBrush}"/>

                <TextBlock Text="Système de Gestion Médicale"
                         FontSize="18"
                         HorizontalAlignment="Center"
                         Margin="0,0,0,50"
                         Foreground="{StaticResource AccentBrush}"/>

                <!-- Login Form -->
                <TextBlock Text="Identifiant"
                         Margin="0,0,0,10"
                         FontSize="16"
                         Foreground="{StaticResource AccentBrush}"/>
                <TextBox Style="{StaticResource ModernTextBoxStyle}"
                        Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                        Margin="0,0,0,30"/>

                <TextBlock Text="Mot de passe"
                         Margin="0,0,0,10"
                         FontSize="16"
                         Foreground="{StaticResource AccentBrush}"/>
                <PasswordBox x:Name="PasswordBox"
                           Style="{StaticResource ModernPasswordBoxStyle}"
                           Margin="0,0,0,40"
                           PasswordChanged="PasswordBox_PasswordChanged"/>

                <Button Content="SE CONNECTER"
                        Style="{StaticResource LoginButtonStyle}"
                        Command="{Binding LoginCommand}"/>

                <!-- Footer -->
                <TextBlock Text="© 2024 Système de Gestion Médicale"
                         Foreground="{StaticResource AccentBrush}"
                         FontSize="12"
                         HorizontalAlignment="Center"
                         Margin="0,40,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window> 